---
paths:
  - "src/**/*.ts"
---

# TypeScript

- No defensive coding. No fallbacks. No `.get()` with defaults. Fail fast, fail loud.
- No `any`. No `as unknown as`. If the type is wrong, fix the type.
- No semaphore patterns, no artificial concurrency limits.
